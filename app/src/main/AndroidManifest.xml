<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myendterm">

    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyEndTerm">
        <activity android:name=".ortho"></activity>
        <activity android:name=".psych" />
        <activity android:name=".Cardio" />
        <activity android:name=".Dent" />
        <activity android:name=".ENTDEP" />
        <activity
            android:name=".EmergencyPage"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".NearHospital"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".diseasePage"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".ConsultPage"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>